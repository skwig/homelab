---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-todo
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-configs

  interval: 1h
  retryInterval: 1m
  timeout: 5m

  path: ./kubernetes/apps/base/todo

  sourceRef:
    kind: GitRepository
    name: flux-system

  prune: false
  deletionPolicy: Orphan
  wait: true
