---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-linkding
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-external-secrets-configs
    - name: infrastructure-cloudnative-pg
    - name: infrastructure-local-path-provisioner
    - name: infrastructure-csi-driver-nfs
    - name: infrastructure-opentelemetry

  interval: 1h
  retryInterval: 1m
  timeout: 5m

  path: ./kubernetes/apps/base/linkding

  sourceRef:
    kind: GitRepository
    name: flux-system

  prune: true
  deletionPolicy: Orphan
  wait: true
