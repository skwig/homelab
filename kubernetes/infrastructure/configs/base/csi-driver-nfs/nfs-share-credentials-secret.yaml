---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nfs-share-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-kv-cluster-secret-store
    kind: ClusterSecretStore
  data:
    - secretKey: username
      remoteRef:
        key: nfs-share-username
    - secretKey: password
      remoteRef:
        key: nfs-share-password
