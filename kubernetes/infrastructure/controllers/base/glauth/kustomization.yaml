apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: glauth
resources:
  - namespace.yaml
  - release.yaml

configMapGenerator:
  - name: glauth-config
    files:
      - config.cfg
