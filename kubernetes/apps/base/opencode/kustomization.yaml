apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opencode

resources:
  - namespace.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - ingress.yaml
  - secret.yaml

configMapGenerator:
  - name: opencode-runtime
    files:
      - flake.nix
