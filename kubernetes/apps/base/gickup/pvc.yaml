---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gickup-backup
spec:
  storageClassName: nfs-share-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
