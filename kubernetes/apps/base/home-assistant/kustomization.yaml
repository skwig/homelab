apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home-assistant
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - database.yaml
  - database-backup-minio.yaml

configMapGenerator:
  - name: configuration-yaml
    files:
      - configuration.yaml
